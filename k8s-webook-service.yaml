apiVersion: v1
kind: Service
metadata:
  name: webook-live
spec:
  selector:
    app: webook-live
  ports:
    - protocol: TCP
      name: http
      port: 10080
      targetPort: 8080
  type: LoadBalancer
  